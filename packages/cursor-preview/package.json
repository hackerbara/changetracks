{
  "name": "@changedown/cursor-preview",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "./dist/bridge.js",
  "types": "./dist/types.d.ts",
  "scripts": {
    "build": "tsc && node build.mjs",
    "build:bundle": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vitest/coverage-v8": "^4.1.0",
    "esbuild": "^0.27.4",
    "happy-dom": "^20.8.4",
    "typescript": "^5.3.3",
    "vitest": "^4.1.0"
  },
  "license": "MIT",
  "dependencies": {
    "@vscode/test-cli": "0.0.11",
    "mocha": "^11.3.0",
    "vite": "8.0.0"
  }
}
