/**
 * Re-export wrapper — canonical implementation lives in changedown.
 */
export { executeCommand, ParseError } from '@changedown/cli/schema-executor';
export type { ToolHandler, CommandDef } from '@changedown/cli/schema-executor';
