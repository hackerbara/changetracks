/**
 * Re-export wrapper — canonical implementation lives in changedown.
 */
export {
  tryParseArgs,
  stringFlag,
  parseIntFlag,
  hasHelpFlag,
  helpResult,
  usageError,
  invalidJsonError,
} from '@changedown/cli/cli-helpers';
